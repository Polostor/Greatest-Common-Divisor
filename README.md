# Greatest Common Divider API

More description in wiki.

**Greatest Common Divider**

Endpoint:
`/gcd`

Request parameters:
`{
    x: "<divisor>",
    y : "<divider>"
}`

Response:
`{
    gcd: "<greatest common divisor>"
}`

Invalid parameters:
`{
    "exception": "<exception text>"
`}
